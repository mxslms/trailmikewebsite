version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: hello-world-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro